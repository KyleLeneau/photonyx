[package]
name = "px-fits"
version = "0.2.0"
rust-version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
px-fs = { workspace = true}

chrono = { workspace = true }
rayon = { workspace = true }
regex = { workspace = true }
strum = { workspace = true }
strum_macros = { workspace = true }
thiserror = { workspace = true }
rustafits = { git = "https://github.com/KyleLeneau/rustafits", branch = "jpg-feature"}

[dev-dependencies]
# Kept only for tests/differential.rs (ADR 006 O3); removed at the end of
# Phase 3 once the native reader has its own image-reading tests to stand
# on and this comparison is no longer needed.
fitsrs = { version = "0.4.1" }

criterion = { workspace = true }
dhat = { workspace = true }
proptest = { workspace = true }
tempfile = { workspace = true }

[[bench]]
name = "header_scan"
harness = false

[[bench]]
name = "full_frame"
harness = false

[[bench]]
name = "region"
harness = false

[[bench]]
name = "write"
harness = false

[lints]
workspace = true
