[alias]
xtask = "run --package xtask --bin xtask --"
lint = "clippy --all-targets -- --cap-lints warn"
px = "run --package px --bin px --"

[env]
CARGO_WORKSPACE_DIR = { value = "", relative = true }
